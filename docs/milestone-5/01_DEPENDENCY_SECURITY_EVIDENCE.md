# Milestone 5 Task 1 dependency security evidence

Date: 2026-09-10. Baseline revision: `1183ebd`.

Acceptance criteria were read from the user-supplied, untracked documents in
`docs/MILESTONE_5_PILOT_LAUNCH/MILESTONE_5_PILOT_LAUNCH/` (README, execution guide,
and Task 1). Those originals and their ZIP were not modified or staged.

## Status

Local dependency verification: PASS. Task 1 overall: FAIL / blocked pending
remote CI and completion of the regression smoke checklist. Task 2 has not begun.
The dependency revision is the commit containing this evidence file and the
package manifest/lockfile; obtain its exact SHA with `git log -1 --format=%H --
docs/milestone-5/01_DEPENDENCY_SECURITY_EVIDENCE.md`.

## Audit and smallest compatible fixes

Baseline `npm audit --json`: 8 affected packages: 1 critical, 3 high, 4 moderate.
The counts are affected packages, not distinct advisories.

| Package | Before | After | Exposure / rationale |
| --- | --- | --- | --- |
| next | 16.3.0 | 16.3.3 | Production framework; first patched 16.3 release |
| eslint-config-next | 16.3.0 | 16.3.3 | Matching framework lint configuration |
| sharp | 0.35.3 | 0.35.4 | Next image processing and vulnerable native image libraries |
| vitest and @vitest/mocker | 4.1.10 | 4.1.11 | Development test tooling |
| fast-uri | 3.1.5 | 3.1.6 | AJV via Sentry webpack tooling and shadcn CLI |
| hono | 4.13.1 | 4.13.5 | MCP SDK via shadcn CLI |
| js-yaml | 4.3.1 | 4.3.2 | ESLint and shadcn configuration loading |
| qs | 6.15.3 | 6.16.0 | Express/body-parser via shadcn MCP SDK |

Shadcn is currently listed in production dependencies even though inspection
found no application imports of these CLI libraries. Findings were patched,
not dismissed by reclassifying packages. No advisory exceptions were accepted.

Advisories and affected ranges reported by the live npm registry audit:

- Next.js critical: [GHSA-p293-qw3h-jr36](https://github.com/advisories/GHSA-p293-qw3h-jr36)
  and [GHSA-2xp9-vwfh-vxw4](https://github.com/advisories/GHSA-2xp9-vwfh-vxw4),
  `>=16.0.0 <16.3.3`. The current advisory pages confirm the patch boundary.
- Sharp high: [GHSA-rgj7-g3m4-5g8c](https://github.com/advisories/GHSA-rgj7-g3m4-5g8c), `<0.35.4`.
- fast-uri high: [GHSA-5jgf-p345-68v8](https://github.com/advisories/GHSA-5jgf-p345-68v8)
  (`>=3.1.3 <3.1.6`), [GHSA-f65p-4m7j-42xc](https://github.com/advisories/GHSA-f65p-4m7j-42xc)
  (`>=3.0.0 <3.1.6`), [GHSA-fph4-wmhf-6fwf](https://github.com/advisories/GHSA-fph4-wmhf-6fwf)
  (`>=3.1.2 <3.1.6`), and [GHSA-jqff-g426-hqxp](https://github.com/advisories/GHSA-jqff-g426-hqxp)
  (`>=3.0.0 <3.1.6`).
- js-yaml high: [GHSA-2883-xcg3-v3hh](https://github.com/advisories/GHSA-2883-xcg3-v3hh), `>=4.0.0 <4.3.2`.
- Vitest/mocker moderate: [GHSA-82fw-gwwq-j7x9](https://github.com/advisories/GHSA-82fw-gwwq-j7x9), `>=2.1.0 <4.1.11`.
- Hono moderate: [GHSA-gqvv-2mrq-wpjv](https://github.com/advisories/GHSA-gqvv-2mrq-wpjv),
  [GHSA-g6gw-c38x-mqfc](https://github.com/advisories/GHSA-g6gw-c38x-mqfc),
  [GHSA-crvj-82cr-hjcx](https://github.com/advisories/GHSA-crvj-82cr-hjcx), all `<4.13.5`.
- qs moderate: [GHSA-x5fp-wj9c-mxmx](https://github.com/advisories/GHSA-x5fp-wj9c-mxmx)
  (`>=6.14.2 <=6.15.3`) and [GHSA-4mjr-xmp4-gh2g](https://github.com/advisories/GHSA-4mjr-xmp4-gh2g)
  (`>=2.2.5 <6.16.0`).

React/react-dom remain 19.2.8, compatible with Next 16.3.3's peer ranges.
Supabase, Stripe, Sentry, Playwright, Vite 8.2.1 and Rolldown 1.2.3 remain unchanged.
The lockfile also updates Next's required SWC helpers, matching compiler packages,
Sharp's native packages, and Vitest's internal packages. npm refreshed PostCSS
8.5.26 to 8.5.28 at the root, retains Vite's 8.5.26, and adds optional Tailwind
WASI metadata. Every resolved download remains on registry.npmjs.org and has
an integrity hash; Linux and Windows Next compiler entries are both 16.3.3.

Only three direct version pins changed. The lockfile was generated by npm;
temporary resolution constraints retained unrelated tooling versions and selected
first-fixed transitive versions, then were removed before clean installation.
There are no persistent overrides or npm security-policy changes.

## Verification and failures investigated

Environment: Windows, Node v24.19.0, npm 12.0.2. `npm.cmd` avoids PowerShell's
blocked `npm.ps1` wrapper. CI uses Node 22 and must still verify that environment.

Baseline `npm run verify` passed secrets, lint, typecheck and all 241 unit tests,
then failed because Docker was stopped. Starting the installed Docker Desktop
restored local PostgreSQL connectivity. Rerunning DB tests exposed existing data
drift in suites 010, 023, 024 and 026. Read-only inspection confirmed the seed's
blocked/out-of-service berths were now available and the expected pricing season
was absent; integration counters also included existing local activity.

No reset, deletion, restore, schema edit or fixture/assertion change was used.
Following the Supabase skill's local CLI workflow, a separate temporary project
was created from unchanged config, migrations, seed.sql, pilot-seed.sql, templates
and tests. Only its project ID and DB/shadow ports changed. Supabase `db start`
initialized this new database from the repository migrations and seeds.

- Isolated project: `berthio-m5-task1-b6f87fd4`.
- Database container: `supabase_db_berthio-m5-task1-b6f87fd4`, local port `55322`.
- Workdir: `C:/Users/Roberts/AppData/Local/Temp/berthio-m5-task1-b6f87fd401fb41759e73082dfd904b81`.
- Existing `supabase_db_berthio` data was retained.
- Baseline isolated DB result: 28 files, 672 assertions PASS.
- Baseline production build: PASS, Next 16.3.0.

Lockfile generation initially failed with npm 12 `EALLOWREMOTE` on an existing
registry.npmjs.org Tailwind tarball. Explicitly selecting that registry did not
resolve it. After verifying all 908 baseline URLs belonged to registry.npmjs.org,
the lockfile-only install/update used `--ignore-scripts --allow-remote=all` for
those commands only. Subsequent plain `npm ci` succeeded without that allowance.

Post-update results:

| Check | Result |
| --- | --- |
| npm ci | PASS; clean install, 769 installed packages |
| npm audit --json | PASS; zero vulnerabilities of any severity |
| npm ls next eslint-config-next react react-dom vitest sharp fast-uri hono js-yaml qs | PASS; expected versions, no invalid peers |
| npm run verify:secrets | PASS |
| npm run lint | PASS |
| npm run typecheck | PASS |
| npm run test | PASS; 37 files, 241 tests |
| npm run test:db | PASS; 28 files, 672 assertions, isolated workdir |
| npm run build | PASS; Next 16.3.3, 34 static pages generated |
| git diff --check | PASS |

The full post-update command was `npm.cmd run verify`, with `SUPABASE_WORKDIR`
set to the isolated workdir above. The existing command and assertions were not
modified. DB tests passed before and after the dependency update on that target;
this does not claim completion of Task 2's full determinism/CI work.

npm 12 reported blocked lifecycle scripts for Sentry CLI and core-js under the
existing allowScripts policy. Required local checks still passed. Deployment
source-map upload/provider behavior is not claimed verified by these results.

## Browser and remote evidence

Focused existing public/auth and admin browser regression checks: PASS, 16 tests
across Chromium desktop and Pixel 7 mobile (51.2 seconds, zero skips). Used
`node --env-file=.env.local node_modules/@playwright/test/cli.js test`
with `E2E_SUPABASE_READY=1`, the existing `public-and-auth.spec.ts` and
`admin-loading.spec.ts`, and the following grep selection:

```text
root redirects|protected dashboard routes|public signup is disabled|callback failures|public availability returns a safe no-fit|booking search reports invalid|sidebar stays mounted|staff navigation keeps
```

These exercised the existing local Supabase API, real local magic-link sessions,
admin navigation through bookings/berths/payments/settings, staff restrictions,
public invalid-input/no-fit behavior and safe auth redirects. They did not mock
provider responses or claim full E2E-suite coverage. Production build had finished
before Playwright started the Next development server.

Mobile `public-pay-at-marina.spec.ts`: PASS, 2 tests, zero skips (11.2 seconds),
using the same environment and `--project=mobile --max-failures=1`. Verified
pay-at-marina confirms once with the full balance outstanding and no Stripe
payment row; the two-method selector defaults to Pay now and switches correctly.
The existing tests clean up their booking/hold and restore the marina's payment
flags to the values confirmed before running: online=true, pay-at-marina=false.

MANUAL ACTION REQUIRED: repository write access and remote CI readback.
`git ls-remote origin refs/heads/main` fails with `Permission denied (publickey)`.
The connected GitHub repository metadata reports `pull: true`, `push: false`.
An HTTPS push dry-run with interactive prompting disabled also failed with
`Invalid username or token` / `Authentication failed`.
No push or CI success is claimed. CI run URL: unavailable for this local revision.

MANUAL ACTION REQUIRED: complete the Task 1 smoke checklist, including password
authentication, a successful availability/mobile step transition into live Stripe
test Checkout, and detailed admin payment-state readback. The automated
pay-at-marina and admin-navigation results above cover parts of that checklist.
No mocked
provider result counts as deployed verification. No external provider configuration
was changed. No database/schema or application behavior changes were made.
